{
  "name": "fieldnote-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Fieldnote Milestone 2 backend — Pl@ntNet identify proxy (2A) + region-pack pipeline (2B) on Cloudflare Workers.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "pipeline:local": "wrangler dev --test-scheduled",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.2",
    "wrangler": "^3.78.0"
  }
}
